doctype
html
    head
        meta(charset='utf-8')
        title opc ua 테스트
    body
        div 소켓 테스트
    script(src="/socket.io/socket.io.js")
    script.
        var socket = io.connect('http://localhost:3000', {
            path: '/socket.io'
        });

        socket.on('connect', function() {
           socket.on('BalluffSignalLightB7', function(data) {
               console.log(data.value)
           });
            socket.on('i24VPowerSupply', function (data) {
                console.log(data.value)
            });
        });